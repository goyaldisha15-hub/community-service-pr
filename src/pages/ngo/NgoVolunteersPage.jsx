import { useEvents } from '../../context/EventContext'
export default function NgoVolunteersPage() { const { events, registrations } = useEvents(); return <section><h1>Volunteer Records</h1><table><thead><tr><th>Name</th><th>Email</th><th>Event</th><th>Attendance</th><th>Check-in Time</th></tr></thead><tbody>{registrations.map((item) => <tr key={item.id}><td>{item.volunteerName}</td><td>{item.volunteerEmail}</td><td>{events.find((event) => event.id === item.eventId)?.title}</td><td>{item.attended ? 'Present' : 'Pending / Absent'}</td><td>{item.checkedInAt ?? '—'}</td></tr>)}</tbody></table>{!registrations.length && <p>No volunteer registrations yet.</p>}</section> }
