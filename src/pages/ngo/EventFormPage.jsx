import { useNavigate } from 'react-router-dom'
import { useState } from 'react'
import { useAuth } from '../../context/AuthContext'
import { useEvents } from '../../context/EventContext'
export default function EventFormPage() { const { createEvent } = useEvents(); const { getNgoMembers } = useAuth(); const navigate = useNavigate(); const members = getNgoMembers(); const [form, setForm] = useState({ title: '', description: '', date: '', time: '', location: '', capacity: 30, memberIds: [], memberDesignation: 'NGO Member' }); function submit(event) { event.preventDefault(); createEvent({ ...form, capacity: Number(form.capacity) }); navigate('/ngo/events') } function selectMembers(event) { setForm({ ...form, memberIds: Array.from(event.target.selectedOptions, (option) => option.value) }) } return <section><h1>Create Event</h1><p>Assign NGO Members now, or add them later from Team Management or Event Details.</p><form className="form-grid" onSubmit={submit}><label>Event title<input value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} required /></label><label>Description<input value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} required /></label><label>Date<input type="date" value={form.date} onChange={(event) => setForm({ ...form, date: event.target.value })} required /></label><label>Time<input type="time" value={form.time} onChange={(event) => setForm({ ...form, time: event.target.value })} required /></label><label>Location<input value={form.location} onChange={(event) => setForm({ ...form, location: event.target.value })} required /></label><label>Capacity<input type="number" min="1" value={form.capacity} onChange={(event) => setForm({ ...form, capacity: event.target.value })} required /></label><label>Assign NGO Members<select multiple value={form.memberIds} onChange={selectMembers}>{members.map((member) => <option key={member.id} value={member.id}>{member.name} — {member.email}</option>)}</select></label><label>Designation for selected members<input value={form.memberDesignation} onChange={(event) => setForm({ ...form, memberDesignation: event.target.value })} placeholder="Example: QR Manager" /></label><button>Create event</button></form></section> }
